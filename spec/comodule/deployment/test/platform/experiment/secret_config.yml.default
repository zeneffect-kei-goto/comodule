aws_access_credentials:
  common:
    access_key_id:     !str ACCESSKEYID
    secret_access_key: !str secret
  # if you want use different IAM to each API.
  # cloud_formation:
  #   region: !str ap-northeast-1
  #   access_key_id:     !str ACCESSKEYID
  #   secret_access_key: !str secret
  # sns:
  #   region: !str ap-northeast-1
  #   access_key_id:     !str ACCESSKEYID
  #   secret_access_key: !str secret
  # rds:
  #   region: !str ap-northeast-1
  #   access_key_id:     !str ACCESSKEYID
  #   secret_access_key: !str secret
  # ec2:
  #   region: !str ap-northeast-1
  #   access_key_id:     !str ACCESSKEYID
  #   secret_access_key: !str secret
  # s3:
  #   access_key_id:     !str ACCESSKEYID
  #   secret_access_key: !str secret

# You write here configurations that you don't want to add to your repository.
